{{#each notes as |note|}}
    <LinkTo @tagName="div" class="card-link" @route="notes.edit" @model={{note.id}}>
        <NoteCard @note={{note}} @onDelete={{action "deleteNote"}} />
    </LinkTo>
{{/each}}