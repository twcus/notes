<div id="card-grid" class="card-grid">
    {{#each @notes as |note|}}
        <Note::Card
            @note={{note}}
            @onDelete={{this.deleteNote}}
        />
    {{/each}}
</div>
{{outlet}}
