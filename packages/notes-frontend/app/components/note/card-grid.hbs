<div id="card-grid" class="card-grid">
    {{#each @notes as |note|}}
        <Note::Card
            @note={{note}}
            @onDelete={{fn this.deleteNote note}}
        />
    {{/each}}
</div>
{{outlet}}
