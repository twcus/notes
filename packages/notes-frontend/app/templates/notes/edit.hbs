<EditNoteCard
    @model={{model}}
    @onSave={{action "saveNote"}}
    @onUndo={{action "undoChanges"}}
    @onDelete={{action "deleteNote"}}
    @onSelectTag={{action "addTag"}}
    @onCreateTag={{action "createTag"}}
    @getAllTags={{action "getAllTags"}}
/>
