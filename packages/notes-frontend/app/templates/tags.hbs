<div class="content">
    <div class="tag-list-editable">
        {{#each @model as |tag|}}
            <Tags::Tag @tag={{tag}} @isEditable={{true}} @onSave={{this.saveTag}} @onDelete={{this.deleteTag}} />
        {{/each}}
    </div>
</div>
