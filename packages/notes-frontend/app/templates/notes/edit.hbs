{{component (concat "notes/editors/" this.activeViewMode.editorComponent)
    note=model.note
    tags=model.tags
    onClose=this.transitionToNotes
    onDelete=this.deleteNote
    onUpdate=this.updatedNote
    onTagCreate=this.createTagAndAddToNote
}}
