<Toolbar />
<div class="content">
    <Note::CardGrid
        @notes={{@model.notes}}
        @tags={{@model.tags}}
        @onDeleteNote={{this.deleteNote}}
        @onUpdate={{this.updatedNote}}
        @saveTask={{this.saveNoteTask}}
    />
</div>
