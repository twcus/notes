{{#if @selectedIcon}}
    <FaIcon @icon={{@selectedIcon}} class="select-option-selected-icon" />
{{/if}}
{{#if @option.icon}}
    <FaIcon @icon={{@option.icon}} class="select-option-icon" />
{{/if}}
<span class="select-option-content">{{get @option @displayProperty}}</span>
