{{#each collections as |collection|}}
    <LinkTo @tagName="div" class="card-link {{if collection.isNew "is-new"}}" @route="collections.edit" @model={{collection.id}}>
        <CollectionCard @collection={{collection}} @onDelete={{action "deleteCollection"}} />
    </LinkTo>
{{/each}}
