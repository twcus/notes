<div id="card-grid" class="card-grid">
    {{#each @notes as |note|}}
        <Note::Card
            @note={{note}}
            @tags={{@tags}}
            @onDelete={{this.deleteNote}}
            @onUpdate={{@onUpdate}}
            @onTagCreate={{@onTagCreate}}
        />
    {{/each}}
</div>
{{outlet}}
