<CollectionEditModal
    @collection={{model}}
    @onSaveAndClose={{action "createCollection"}}
    @onSelectTag={{action "addTag"}}
    @onCreateTag={{action "createTag"}}
    @onGetAllTags={{action "getAllTags"}}
/>
