<div class="list-pane">
    {{#each @notes as |note|}}
        <LinkTo @tagName="div" @route="notes.edit" @model={{note.id}} class="list-item pell-content {{if this.isActive "active"}}">
            {{{note.content}}}
        </LinkTo>
    {{/each}}
</div>
