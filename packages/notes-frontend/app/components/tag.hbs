<span class="tag">
    <span class="tag-text">{{@tag.content}}</span>
    {{#if @hoverActionIcon }}
        <button class="button">
        <FaIcon @icon={{@hoverActionIcon}} />
    </button>
    {{/if}}
</span>